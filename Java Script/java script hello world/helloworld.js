/**
* JS - Hello World
* @author José de Assis
* www.professorjosedeassis.com.br
*/

function hello(){
	// a linha abaixo cria uma variável chamada nome
	var nome;
	// a linha abaixo cria uma caixa de entrada de dados
	// o valor digitado é atribuido(set) a variável nome
	nome = prompt("Qual é o seu nome ?");
	// a linha abaixo cria uma caixa de diálogo
	// + nome concatena("junta") um texto com o conteúdo
	// da variável nome
	alert ("Hello " + nome);
}


